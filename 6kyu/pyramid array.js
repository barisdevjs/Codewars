/* function pyramid(n) {
    let res = [];
    for(let i = 0; i < n; i++)
      res.push([...Array(i+1)].fill(1));
      res
    return res;
  }
console.log(pyramid(3)) */

/* function pyramid(n) {
    return Array(n).fill().map((e,i)=>Array(i+1).fill('s'))
}
console.log(pyramid(3)) */

